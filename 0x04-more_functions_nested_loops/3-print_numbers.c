#include "main.h"
/**
 * print_numbers - function that prints the numbers
 * Return: always 0
 */
void print_numbers(void)

{

	int i;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
}
