#include <stdio.h>
/**
 * main - Entry point
 * Description: 'display alphabets in lowercase using putchar only'
 * Return: 0
 */
int main(void)
{
	int n = 'a';
	while (n <= 'z')
	{
		putchar(n);
		l++;
	}
	putchar('\n');
	return (0);
}
